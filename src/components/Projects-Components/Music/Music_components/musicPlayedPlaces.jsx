import React from 'react'

export default function musicPlayedPlaces({logo, instruments, years}) {
  return (
    <div>
        <img src={logo} />

        <div>
            
        </div>
    </div>
  )
}
